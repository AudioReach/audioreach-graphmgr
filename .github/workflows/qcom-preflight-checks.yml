name: Qualcomm Preflight Checks

on:
  pull_request:
    branches: [ abi ]
  push:
    branches: [ abi ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@abi
    secrets: inherit
