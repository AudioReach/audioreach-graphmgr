name: Qualcomm Preflight Checks

on:
  pull_request_target:
    branches: [ Axiom ]
  push:
    branches: [ Axiom ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: AudioReach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@Axiom
    secrets: inherit
